@model IEnumerable<InventarioLPS.Data.Entities.CorreoAdicionalProveedor>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Correo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProveedorNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Correo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProveedorNavigation.Correo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCorreo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCorreo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCorreo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
